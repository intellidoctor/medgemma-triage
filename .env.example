# GCP Authentication — base64-encoded service account JSON
GOOGLE_APPLICATION_CREDENTIALS_BASE64=<base64-encoded-service-account-json>

# MedGemma 27B Text (clinical reasoning, text-only)
# Format: https://{DEDICATED_DNS}/v1beta1/projects/{PROJECT_NUMBER}/locations/{REGION}/endpoints/{ENDPOINT_ID}
MEDGEMMA_27B_BASE_URL=https://<ENDPOINT_DNS>.prediction.vertexai.goog/v1beta1/projects/<PROJECT_NUMBER>/locations/<REGION>/endpoints/<ENDPOINT_ID>

# MedGemma 4B Multimodal (images + text)
MEDGEMMA_4B_BASE_URL=https://<ENDPOINT_DNS>.prediction.vertexai.goog/v1beta1/projects/<PROJECT_NUMBER>/locations/<REGION>/endpoints/<ENDPOINT_ID>

# --- MCP Server Credentials ---

# GitHub MCP — each team member creates their OWN PAT
# Create at: https://github.com/settings/tokens (classic token)
# Required scopes: repo, read:org, project
GITHUB_PERSONAL_ACCESS_TOKEN=<your-github-personal-access-token>

# Slack MCP — shared bot token (same for everyone, ask Roberto)
SLACK_BOT_TOKEN=<slack-bot-token>
SLACK_TEAM_ID=<your-slack-team-id>
